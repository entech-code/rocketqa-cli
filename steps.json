{
  "given": [],
  "when": [
    "navigate to {string}",
    "enter {string} into {locator} field",
    "clear {locator} field",
    "type {string} into {locator} field",
    "hover over {locator}",
    "switch to tab {int}",
    "close tab {int}",
    "click {locator}",
    "click {locator} if {locator} is visible",
    "force click {locator}",
    "click {locator} within frame {locator}",
    "right-click {locator}",
    "middle-click {locator}",
    "press {string} key",
    "double-click {locator}",
    "check {locator}",
    "uncheck {locator}",
    "clear {locator}",
    "move mouse to {locator}",
    "scroll mouse wheel X {int} and Y {int}",
    "focus on {locator}",
    "the DOM is fully loaded",
    "wait {int} milliseconds",
    "hold down {string} key",
    "press complex key combination {string}",
    "release {string} key",
    "run scenario {scenario}"
  ],
  "then": [
    "{locator} is visible",
    "{locator} is visible in frame {locator}",
    "{locator} is not visible",
    "{locator} is hidden",
    "{locator} is checked",
    "{locator} is editable",
    "{locator} is enabled",
    "{locator} is disabled",
    "{locator} is empty",
    "{locator} contains text {string}",
    "text field {locator} contains {string}",
    "there are {int} tabs"
  ]
}